# DECISIONS_LOG.md

# سجل القرارات

يسجل هذا الملف القرارات المهمة التي تؤثر على النطاق، التنفيذ، التصميم، المخاطر، أو طريقة العمل.

## السجل

| Decision ID | Date | Decision | Reason | Impact | Approved By | Linked Task / Issue |
|---|---|---|---|---|---|---|
| DEC-0001 | 2026-06-27 | Use Project Control Layer before implementation. | Execution needs traceability, auditability, task history, and issue tracking. | All future implementation tasks require TASK-ID and review status. | Tera Agent / Project Owner | TASK-0000 |
| DEC-0002 | 2026-06-27 | Sync preparation status before implementation. | Some preparation files still described earlier pending states after implementation and testing files were created. | Project is marked preparation-complete and implementation-pending-approval. | Tera Agent | ISSUE-0001 |
| DEC-0003 | 2026-06-27 | Require sub-agent handbacks to be recorded inside the related task file. | TASK-0001 exposed that an EngineeringAgent handback can remain only in chat unless the protocol explicitly requires recording. | Tasks cannot move to Accepted or Closed until the sub-agent handback is documented in `project-control/tasks/[TASK-ID].md`. | Tera Agent / Project Owner | ISSUE-0002 |
| DEC-0004 | 2026-06-27 | Mandatory secret handling rules across Tera system. | TASK-0003 exposed real password as fallback value in `prisma.config.ts` and in task file content. Core rules were insufficient. | Real secrets permitted ONLY in `.env`/`.env.local`/local env vars. Banned from: project-control, project-preparation, generated-agents, tera-system, task files, logs, handbacks, code/config fallbacks. `SECRET_EXPOSURE` rejection code added. Pre-Execution Gate items 10-11 added; Post-Execution Review Gate items 11-13 added. AGENT_GENERATION_TEMPLATE.md handback template now includes `Secrets Handling` field. Handbacks must redact secrets. | Project Owner / Tera Agent | ISSUE-0003 |
| DEC-0005 | 2026-06-27 | Use `project-control/TERA_ACTIVE_CONTEXT.md` as the startup handoff for new sessions in ongoing projects. | New sessions should not reopen the whole system and project context by default. A compact startup context reduces token usage and makes resumption more consistent. | Tera now reads `TERA_ACTIVE_CONTEXT.md` first when it exists, then reads only the official files needed for the current task. The file must remain short, non-archival, secret-free, and updated after major tasks or phase closures. | Project Owner / Tera Agent | N/A |
| DEC-0006 | 2026-06-27 | Require SecurityAgent independent review for tasks involving Auth, JWT, passwords, or secrets before next phase. | TASK-0004 Post-Execution Review re-evaluation (30-item checklist) revealed: (1) unclassified deviation (logout/route.ts), (2) ISSUE-0003 secondary breach, (3) no explicit independent review decision. New rules require explicit Independent Review Decision per Item 30. | TASK-0004 requires SecurityAgent independent security review before TASK-0005 proceeds. Deviation classification must be explicit. | Tera Agent | TASK-0004 / ISSUE-0003 |
| DEC-0007 | 2026-06-27 | Post-Execution Review must inspect core `project-control` files and require an explicit independent-review decision when relevant. | TASK-0004 exposed that Tera can miss secret leakage or traceability issues if it reviews implementation output without fully reviewing task/control records. | Tera must now review the task file plus `TASK_REGISTRY.md`, `PROJECT_ACTIVITY_LOG.md`, `PROJECT_STATE.md`, `ISSUES_AND_GAPS.md`, `DECISIONS_LOG.md`, and `TERA_ACTIVE_CONTEXT.md` if it exists. Secret leakage in reports/logs/tasks now fails review. Out-of-target changes must be classified. Tera must explicitly decide whether `ProjectControlAgent`, `SecurityAgent`, or `QAAndAcceptanceAgent` should perform an independent follow-up review. | Project Owner / Tera Agent | ISSUE-0003 |
| DEC-0008 | 2026-06-27 | Split EngineeringAgent into EngineeringAgent (Backend/Logic) + FrontendAgent (UI/Styling). | EngineeringAgent became overloaded handling both Server Actions/Business Logic and UI/Styling. Parallel work becomes possible. UI consistency across screens improves with a dedicated agent enforcing 28_UI_UX_GUIDELINES.md. | EngineeringAgent now: actions.ts, Prisma, Auth, Middleware, Config, DB, Business Logic ONLY. FrontendAgent now: page.tsx, CSS, RTL, Modal, Toast, UI consistency, enforcing 28_UI_UX_GUIDELINES.md. Both agents can work in parallel on the same screen. | Tera Agent / Project Owner | N/A |
| DEC-0009 | 2026-06-27 | Activate selected generated agents inside `.opencode/agents/` for upcoming implementation phases. | The next phases need faster delegation and parallel work without creating generic agents each time. The old active EngineeringAgent was outdated and still allowed UI work after DEC-0008. | Installed/updated active agents: EngineeringAgent, FrontendAgent, ProjectControlAgent, QAAndAcceptanceAgent, BusinessWorkflowAgent, UIUXStructureAgent, ReportingAnalyticsAgent, DocumentationHandoverAgent. Kept SecurityAgent and tera. Did not activate RequirementsScopeAgent or DataDesignAgent because no scope/data redesign is currently planned. | Tera Agent / Project Owner | N/A |
| DEC-0010 | 2026-06-27 | Unify sub-agent lifecycle and activation policy. | Older notes could be read as either "use only current active agents" or "pre-activate future agents broadly". The system needs one operational rule. | Tera now identifies agents needed now and likely needed later, generates drafts first in `generated-agents/opencode/`, activates inside `.opencode/agents/` only after strict specialization, may generate additional agents later when needed, and must ask the user to restart the current environment after any new activation. | Tera Agent / Project Owner | DEC-0009 |
| DEC-0011 | 2026-06-27 | Add `requireAdmin()` authorization check inside all Banks Server Actions for Defense in Depth. | Post-TASK-0007 review identified that `app/banks/actions.ts` had zero authorization checks — only route-level middleware protected create/update/delete operations. Middleware alone is practically sufficient but lacks defense in depth for Server Action direct invocation or middleware misconfiguration. | Added `requireAdmin()` to `lib/auth.ts` — a simple helper that verifies session exists and role is ADMIN. All 4 Banks Server Actions (listBanks, createBank, updateBank, deleteBank) now call `requireAdmin()` at the top. `page.tsx` updated to handle the `{ error: string }` return case from listBanks. This sets the security pattern for all future Server Actions (Parties S04, Users S05, Checks S02). | Tera Agent | TASK-0007 |
