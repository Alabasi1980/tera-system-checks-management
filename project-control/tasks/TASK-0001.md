# TASK-0001: Scaffold Next.js + TypeScript + PostgreSQL + Prisma (أساس تقني فقط)

## Task Info

| البند | القيمة |
|---|---|
| **Task ID** | TASK-0001 |
| **Title** | Scaffold Next.js + TypeScript + إعداد Prisma + إنشاء `.env.example` |
| **Phase** | المرحلة 1 — إعداد الأساس التقني |
| **Assigned To** | EngineeringAgent |
| **Status** | Draft |
| **Linked Files** | — |
| **Priority** | عالية (أول مهمة تنفيذية) |

## Objective

إنشاء مشروع Next.js + TypeScript عامل محليًا، وإعداد Prisma للاتصال بـ PostgreSQL، وإنشاء ملف `.env.example` فقط (بدون `.env` فعلي وبدون أي نموذج بيانات).

هذه المهمة هي **أصغر وحدة تنفيذية آمنة** — تقوم فقط بتجهيز البيئة التقنية ولا تحتوي على أي Schema أو UI أو Auth.

## الأهداف المحددة

1. Scaffold مشروع Next.js + TypeScript في مجلد `checks-management` داخل الـ workspace.
2. تثبيت حزمة `prisma` و `@prisma/client`.
3. إنشاء ملف `prisma/schema.prisma` أساسي (بدون أي `model`) يحتوي فقط على:
   - `generator client` مع `provider = "prisma-client-js"`
   - `datasource db` مع `provider = "postgresql"` و `url = env("DATABASE_URL")`
4. إنشاء ملف `.env.example` يحتوي فقط على المتغير `DATABASE_URL` مع قيمة placeholder آمنة:
   ```
   DATABASE_URL="postgresql://user:password@localhost:5432/checks_management"
   ```
5. التأكد أن المشروع يعمل بأمر `npm run dev` (أو `yarn dev` / `pnpm dev` حسب ما يختاره الأمر).

## ما هو خارج النطاق (ممنوع في هذه المهمة)

- ❌ إنشاء أي `model` داخل `schema.prisma`
- ❌ إنشاء أي جدول أو Schema
- ❌ تشغيل `npx prisma db push`
- ❌ تشغيل `npx prisma migrate`
- ❌ تشغيل `npx prisma generate`
- ❌ اختبار اتصال فعلي بقاعدة البيانات
- ❌ إنشاء `.env` بقيم فعلية
- ❌ UI / شاشات / واجهة مستخدم
- ❌ API Routes
- ❌ Authentication / Authorization
- ❌ Seed Data
- ❌ Docker / CI-CD
- ❌ أي توثيق إضافي (README, إلخ)
- ❌ تعديل `tera-system/`, `project-preparation/`, `project-control/`, `generated-agents/`

## Allowed Write Targets

- إنشاء مجلد `checks-management/` داخل الـ workspace (Next.js project)
- جميع الملفات التابعة لمشروع Next.js (package.json, tsconfig.json, إلخ)
- ملف `prisma/schema.prisma` (بدون models)
- ملف `.env.example`

ممنوع الكتابة في:
- أي ملف خارج مجلد `checks-management/`
- `project-preparation/`, `tera-system/`, `project-control/`, `generated-agents/`

## Reference Files

- `project-preparation/08_TECHNICAL_ARCHITECTURE.md` — القرارات التقنية (Next.js + TypeScript + PostgreSQL + Prisma)
- `project-preparation/09_IMPLEMENTATION_PLAN.md` — مراحل التنفيذ (المرحلة 1)
- `project-preparation/28_UI_UX_GUIDELINES.md` — للعلم (لا واجهة في هذه المهمة)

## Constraints

- لا يتم إنشاء أي `model` في `schema.prisma`.
- لا يتم إنشاء `.env` بقيم فعلية — فقط `.env.example`.
- لا يتم تشغيل أي أمر `prisma` ينتج عنه `.env` (مثل `npx prisma init`).
- يجب إنشاء `schema.prisma` يدويًا وليس عبر `prisma init`.
- لا يتم تشغيل `npm run build` — فقط `npm run dev` للتحقق من بدء التشغيل.
- EnginneringAgent لا يقرأ كودًا غير مرتبط بالمهمة.
- EnginneringAgent لا يضيف حزمًا أو مكتبات غير مطلوبة مباشرة.

## CLI Commands المقترحة ونطاقها

| الأمر | الأثر المسموح | الأثر الممنوع |
|---|---|---|
| `npx create-next-app@latest checks-management --typescript` | إنشاء مشروع Next.js + TypeScript مع الهيكل الأساسي | إضافة أي إعدادات إضافية غير مطلوبة |
| `npm install prisma @prisma/client` (داخل مجلد المشروع) | تثبيت Prisma و Client | لا شيء |
| إنشاء `prisma/schema.prisma` يدويًا | ملف أساسي بدون models | إنشاء أي model أو جدول |
| إنشاء `.env.example` يدويًا | قالب placeholder آمن | إنشاء `.env` بقيم فعلية |

**ممنوع نهائيًا:** `npx prisma init` (لأنه ينشئ `.env` افتراضيًا).

## Acceptance Criteria

1. يوجد مجلد `checks-management/` يعمل كمشروع Next.js + TypeScript.
2. المشروع يبدأ بـ `npm run dev` ويعرض صفحة الترحيب الافتراضية لـ Next.js في المتصفح.
3. `prisma` و `@prisma/client` مثبتان في `package.json`.
4. يوجد ملف `prisma/schema.prisma` يحتوي على `generator client` و `datasource db` فقط — لا يوجد أي `model`.
5. يوجد ملف `.env.example` يحتوي على `DATABASE_URL` ولا يحتوي على `.env` فعلي.
6. لا توجد ملفات إضافية غير مطلوبة (README إضافي، إلخ).

---

## Delegation Plan

| البند | القيمة |
|---|---|
| Requested Agent | EngineeringAgent |
| Context Type | Task Context + reference files |
| Token Budget | Low-Medium |
| Expected Output Limit | Brief summary + confirmation of deliverables |
| Approval Required | Yes (before execution) |

## Delegation Format (to EngineeringAgent)

```
Task ID: TASK-0001
Agent: EngineeringAgent
Stage: Phase 1 — Technical Foundation
Objective: إنشاء مشروع Next.js + TypeScript عامل، إعداد Prisma، إنشاء .env.example
Reference Files:
- project-preparation/08_TECHNICAL_ARCHITECTURE.md
- project-preparation/09_IMPLEMENTATION_PLAN.md
Allowed Write Targets:
- إنشاء مجلد checks-management/ وجميع ملفات Next.js داخله
- prisma/schema.prisma (بدون models)
- .env.example
Constraints:
- لا Prisma models
- لا .env بقيم فعلية
- لا prisma init
- لا db push / migrate / generate
- لا UI / API / Auth
Token Budget: Low-Medium
Expected Output Limit: Brief confirmation + files created
```

---

## Pre-Execution Gate Result

| # | Check | Result | Notes |
|---|---|---|---|
| 1 | مرتبطة بمرحلة معتمدة في خطة التنفيذ؟ | ✅ PASS | المرحلة 1 من `09_IMPLEMENTATION_PLAN.md` |
| 2 | أصغر وحدة تنفيذية ممكنة؟ | ✅ PASS | Scaffold فقط — لا Schema ولا UI ولا Auth |
| 3 | هدف واحد فقط؟ | ✅ PASS | إعداد البيئة التقنية فقط |
| 4 | يوجد عنصر يمكن تأجيله؟ | ✅ PASS (No) | لا يوجد; كل شيء مطلوب للمرحلة 1 |
| 5 | تضيف UI دون أن تكون مهمة UI؟ | ✅ PASS (No) | لا يوجد UI |
| 6 | تضيف API Route دون طلب صريح؟ | ✅ PASS (No) | لا يوجد API |
| 7 | تضيف Auth دون طلب صريح؟ | ✅ PASS (No) | لا يوجد Auth |
| 8 | تضيف Prisma models دون أن تكون مهمة Schema؟ | ✅ PASS (No) | schema.prisma أساسي بدون models |
| 9 | تنفذ migration أو db push؟ | ✅ PASS (No) | ممنوع صراحة |
| 10 | تنشئ `.env` بقيم فعلية؟ | ✅ PASS (No) | فقط `.env.example` |
| 11 | تضيف مكتبات غير مطلوبة؟ | ✅ PASS (No) | فقط Next.js + TypeScript + Prisma |
| 12 | تكتب خارج Allowed Write Targets؟ | ✅ PASS (No) | محصور في `checks-management/` |
| 13 | تعدّل tera-system أو project-preparation؟ | ✅ PASS (No) | ممنوع صراحة |
| 14 | الأوامر تحتاج موافقة قبل التنفيذ؟ | ✅ PASS (Yes) | تعرض للاعتماد حاليًا |
| 15 | فحص الآثار الجانبية للأوامر؟ | ✅ PASS | تم فحص كل أمر — `prisma init` ممنوع |
| 16 | يوجد أمر ينشئ/يعدل ملفات خارج النطاق؟ | ✅ PASS (No) | جميع الأوامر ضمن النطاق |
| 17 | تناقض بين القيود والمخرجات؟ | ✅ PASS (No) | متسق |
| 18 | معايير القبول قابلة للاختبار؟ | ✅ PASS | قابلة للفحص: وجود ملفات + `npm run dev` |
| 19 | يوجد مسار تراجع آمن؟ | ✅ PASS | حذف مجلد `checks-management/` وإعادة المحاولة |

### CLI / Tool Side Effects Gate

| الأمر | الأثر | القرار | السبب |
|---|---|---|---|
| `npx create-next-app@latest` | إنشاء هيكل Next.js + TypeScript | ✅ مسموح | ضمن النطاق |
| `npm install prisma @prisma/client` | تعديل package.json + node_modules | ✅ مسموح | ضمن النطاق |
| إنشاء `prisma/schema.prisma` يدويًا | ملف بدون models | ✅ مسموح | بدون `prisma init` |
| إنشاء `.env.example` يدويًا | قالب placeholder | ✅ مسموح | لا `.env` فعلي |
| `npx prisma init` | ينشئ `.env` | ❌ **ممنوع** | مستبدل بإنشاء يدوي |

**Gate Status: PASS ✅**

**Required Action:**
- عرض المهمة للاعتماد قبل التفويض إلى EngineeringAgent.
- بعد الاعتماد، تفويض EngineeringAgent لتنفيذ TASK-0001.

---

## Rollback Plan

إذا فشلت عملية Scaffold:
1. حذف مجلد `checks-management/` بالكامل.
2. التحقق من إصدار Node.js و npm.
3. إعادة المحاولة بعد التأكد من البيئة.
4. إذا استمر الفشل، إعادة الحالة إلى Tera مع توضيح الخطأ.

---

## EngineeringAgent Handback — تسليم العميل الفرعي

```text
Task ID: TASK-0001
Agent: EngineeringAgent
Status: Done
Files Produced or Updated:
- checks-management/package.json (مُحدّث — أُضيف prisma, @prisma/client)
- checks-management/prisma/schema.prisma (جديد — generator client + datasource db فقط)
- checks-management/.env.example (جديد — DATABASE_URL)

Summary:
- تم إنشاء مشروع Next.js 16.2.9 + TypeScript كاملًا باستخدام create-next-app
- تم تثبيت prisma@^7.8.0 و @prisma/client@^7.8.0
- تم إنشاء prisma/schema.prisma يدويًا (دون استخدام npx prisma init)
- تم إنشاء .env.example مع DATABASE_URL template
- تم التحقق من npm run dev يعرض صفحة Next.js الافتراضية (HTTP 200)
- التأكد من عدم وجود ملف .env فعلي

Assumptions:
- Tailwind CSS تم تثبيته تلقائيًا من create-next-app الافتراضي
- المنفذ 3000 كان مشغولًا، تم الاختبار على المنفذ 3100

Issues or Missing Information:
- لا توجد

Decisions Needed from Tera:
- لا شيء

Recommendation:
- المهمة مكتملة وجاهزة. يمكن البدء في TASK-0002 عند الحاجة.
```

## Tera Review — مراجعة تيرا

| بند المراجعة | النتيجة |
|---|---|
| هل المخرجات ضمن النطاق المسموح؟ | ✅ نعم — فقط checks-management/ |
| هل تم تجنب Prisma models؟ | ✅ نعم — schema.prisma بدون models |
| هل تم تجنب `.env` فعلي؟ | ✅ نعم — فقط .env.example |
| هل تم تجنب UI / API / Auth؟ | ✅ نعم |
| هل `npm run dev` يعمل؟ | ✅ نعم — تم التحقق |
| هل هناك أي تجاوز للنطاق؟ | ✅ لا |

**حالة Tera: Accepted ✅**
**تاريخ المراجعة:** 2026-06-27
